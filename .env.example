# このファイルを .env にコピーして値を設定してください
# ローカル開発では BASIC_AUTH_PASS を空にすると認証をスキップします

# Basic認証（公開時に設定）
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=

# セッション暗号化キー（本番では必ず設定）
SECRET_KEY=

# CSRFトークン（本番では必ず設定）
CSRF_SECRET=

# 画像保存先（Renderでは /tmp/downloads を推奨）
DOWNLOAD_DIR=downloads
